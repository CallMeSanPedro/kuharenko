<!--
Колонки «Состояние» и «Статус эксплуатации».
Binding замените на фактическое свойство строки.
Остальные колонки оставьте DataGridTextColumn.
-->
<DataGridTemplateColumn Header="Состояние" Width="120">
    <DataGridTemplateColumn.CellTemplate>
        <DataTemplate>
            <ContentControl Content="{Binding Condition}"
                            ContentTemplate="{DynamicResource StatusBadgeTemplate}"
                            Focusable="False"
                            VerticalAlignment="Center"/>
        </DataTemplate>
    </DataGridTemplateColumn.CellTemplate>
</DataGridTemplateColumn>

<DataGridTemplateColumn Header="Статус эксплуатации" Width="150">
    <DataGridTemplateColumn.CellTemplate>
        <DataTemplate>
            <ContentControl Content="{Binding Status}"
                            ContentTemplate="{DynamicResource StatusBadgeTemplate}"
                            Focusable="False"
                            VerticalAlignment="Center"/>
        </DataTemplate>
    </DataGridTemplateColumn.CellTemplate>
</DataGridTemplateColumn>
